#include "game.hpp"
#include <thread>

int main()
{
    Game game;
    game.run();
}
/*
if (ex::is_key_down(app.window(), KEY_W))
        {
            std::cout << "W is being pressed" << std::endl;
        }
*/